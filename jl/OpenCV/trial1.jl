# well this is the tria,on opencv learning 
# well this is the library for image rendering soo just keep intouch

#starting bu using the library opencv
using OpenCV

cv = OpenCV
#then taking the image well it is called loading the image
img = cv.imread("./image.png")

#well its tme to print out the image
cv.imgshow("well image")

#i literary dont know what is this but am just placing it
cv.waitkey(int64(0))